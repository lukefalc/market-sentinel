# Scripts

This folder is for small helper scripts, such as running analysis jobs, setting
up a local database, or generating reports.

Keep scripts thin. Most reusable logic should live in `src/market_sentinel`.
