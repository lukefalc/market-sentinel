"""Command-line entry point for market-sentinel.

The command-line interface will eventually let users run common tasks such as
loading data, calculating analytics, generating reports, and checking alerts.
For now, it only confirms that the project has been installed correctly.
"""


def main() -> None:
    """Run the market-sentinel command-line interface placeholder."""
    print("market-sentinel is installed. Data downloading is not implemented yet.")
