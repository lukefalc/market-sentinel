# Configuration

This folder is for user-editable configuration files, such as ticker lists,
report settings, and alert thresholds.

Secrets should go in `.env`, not in files committed to the repository.
